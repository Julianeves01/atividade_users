class Post {
        constructor(imagem, likes, comentarios) {
            this.imagem = imagem;
            this.likes = likes;
            this.comentarios = comentarios;
        }
    }

module.exports = Post;